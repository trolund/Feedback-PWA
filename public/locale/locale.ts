// Set the key-value pairs for the different languages you want to support.
import dk from "./dk.json";
import en from "./dk.json";

const locale = {
  en: en,
  da: dk,
  "da-DK": dk
};

export default locale;
